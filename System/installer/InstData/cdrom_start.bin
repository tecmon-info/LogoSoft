#!/bin/sh

# Linux Shell Script
# Sucht den CD-ROM-Pfad und startet die Anwendung von der CD-ROM aus

cd_path=$(mount | grep "iso9660" | awk '{ print $3; }')

echo $cd_path

$cd_path/Linux/Tools/Application/LOGOComfort